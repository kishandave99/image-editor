(function(window) {
    window.environment = window.environment || {};

    // API url
    window.environment.webService = 'https://image-editor99.netlify.app/api/';

    // Base url
    // window.environment.baseUrl = '/image-editor';
    window.environment.baseUrl = '';

    // Public Resource url
    window.environment.publicResourceUrl = 'https://image-editor99.netlify.app';

}(this));